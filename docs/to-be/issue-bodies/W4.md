## Objective

Reconstruct GitHub Actions workflow/job/matrix/attempt/step execution graphs and calculate work duration, wall-clock, and critical path.

## Dependencies

- Blocked-by: #2
- Blocks: #8

## Position in Graph

```mermaid
flowchart LR
    W0["W0 Evidence Contracts"] --> W4["W4 GitHub DAG"] --> W6["W6 Reports"]
```

## Scope

GitHub API pagination/rate limits, logical/execution DAGs, queue/execute/idle time, matrix, skipped, retry, cancelled, failed dependency, reusable workflow, and missing timestamp handling.

## Expected Touch Points

- `src/pipeline_toolkit/github/`
- `src/pipeline_toolkit/pipeline/`
- `tests/fixtures/github-actions/`
- `tests/fixtures/dag/`

## Acceptance Criteria

- [ ] Matrix, skipped, retry, cancelled, failed dependency, reusable workflow, and missing timestamp cases are represented.
- [ ] Queue, execution, work, wall-clock, and critical path are distinct metrics.
- [ ] Serial, parallel, diamond, idle-gap, retry, and skipped fixtures match expected values.
- [ ] API pagination and 401/403/404/429/5xx behavior are explicit.
- [ ] Critical-path output contains node and workflow/job references.

## Verification

Fixture-driven DAG tests and GitHub API client tests with pagination, rate-limit, and partial-response cases.
