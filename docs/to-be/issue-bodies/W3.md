## Objective

Implement JUnit, pytest, Gradle Profiler, system, and Docker BuildKit collectors.

## Dependencies

- Blocked-by: #3
- Blocks: #8

## Position in Graph

```mermaid
flowchart LR
    W1["W1 Command Runner"] --> W3["W3 Collectors"] --> W6["W6 Reports"]
```

## Scope

JUnit XML, pytest XML/durations, Gradle Profiler, CPU/peak memory, Docker cache/build metadata, parser warnings, capability detection, and normalized provenance.

## Expected Touch Points

- `src/pipeline_toolkit/collectors/`
- `src/pipeline_toolkit/inspect/`
- `tests/fixtures/junit/`
- `tests/fixtures/pytest/`

## Acceptance Criteria

- [ ] JUnit and pytest expose counts, failures, skips, duration, and slowest tests.
- [ ] Gradle/system collectors preserve CPU and peak memory when available.
- [ ] Docker collector records build duration, cache signals, and image metadata.
- [ ] Malformed, missing, and partial inputs produce explicit statuses.
- [ ] Outputs validate against W0 schemas and preserve raw provenance.

## Verification

Parser unit tests, fixture integration tests, malformed-input tests, and missing-artifact tests.
