## Objective

Resolve safe previous and explicit baselines from validated manifests.

## Dependencies

Blocked by the manifest contract issue.

## Acceptance Criteria

- `previous` returns the closest earlier successful release with exact environment/suite/config/runner compatibility.
- Explicit tag lookup never falls back silently.
- Missing, duplicate, incompatible or insufficient evidence returns `inconclusive` with machine-readable reasons.

## Verification

`PYTHONPATH=src python3 -m pytest -q tests/test_trend_selector.py tests/test_trend_service.py`
