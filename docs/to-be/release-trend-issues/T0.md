## Objective

Record the release trend decision and an implementation-ready PRD/Issue Graph.

## Scope

`ADR-0004`, the release trend PRD, approved design, implementation plan, work graph, and session handoff. No runtime code.

## Acceptance Criteria

- ADR is created only through ADR Toolkit and `validate`/`index` pass.
- The PRD defines identity, compatibility, storage boundary, CLI, reporting, workflow, and test criteria.
- `validate_work_graph.py` reports zero errors and warnings.

## Verification

`adr.py validate`, `adr.py index`, `validate_work_graph.py`, `git diff --check`, and the toolkit verification script.
