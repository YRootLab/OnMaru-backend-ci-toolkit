## Objective

Prove the complete multi-release comparison lifecycle and document consumer usage.

## Dependencies

Blocked by renderer and CLI/workflow issues.

## Acceptance Criteria

- A synthetic history covers a comparable previous release, explicit incompatible release, and failed intermediate run.
- No network/AI call is required by the end-to-end test.
- Full coverage and workflow-security verification pass.

## Verification

`PYTHONPATH=src python3 -m pytest -q tests/test_trend_e2e.py && bash scripts/verify_toolkit.sh`
