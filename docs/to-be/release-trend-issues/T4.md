## Objective

Expose trend commands and a least-privilege reusable workflow convention.

## Dependencies

Blocked by the selector/service issue. This work may proceed in parallel with the renderer issue.

## Acceptance Criteria

- CLI supports `trend compare` with `previous` or explicit baseline and `trend history` over a local history root.
- Reusable workflow materializes manifest files locally and uploads outputs without core GitHub API access.
- Fork PR secret isolation and least-privilege workflow checks pass.

## Verification

`PYTHONPATH=src python3 -m pytest -q tests/test_cli.py tests/test_reusable_benchmark_workflow.py && python3 scripts/verify_workflow_security.py`
