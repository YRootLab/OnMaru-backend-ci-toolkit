## Objective

Render comparison and historical facts deterministically for developer and GitHub audiences.

## Dependencies

Blocked by the selector/service issue.

## Acceptance Criteria

- JSON, Markdown, HTML, and Job Summary show identities, strategy, samples, deltas/status, and omission reasons.
- No output describes an incompatible/failed observation as a performance improvement.
- Same fixture produces byte-stable text output.

## Verification

`PYTHONPATH=src python3 -m pytest -q tests/test_trend_render.py`
